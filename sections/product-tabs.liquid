    {%- style -%}

    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top | times: 0.50 | round: 0 }}px;
      padding-bottom: {{ section.settings.padding_bottom | times: 0.50 | round: 0 }}px;
    }
     @media screen and (min-width: 576px) and (max-width: 749px){
     .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
      padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
    }
     }
    @media screen and (min-width: 750px) {
      .section-{{ section.id }}-padding {
        padding-top: {{ section.settings.padding_top }}px;
        padding-bottom: {{ section.settings.padding_bottom }}px;
      }
    }
   ul.custom--tabs{
          list-style-type: none;
          max-width: 100%;
          display: flex;
          flex-wrap: wrap;
          padding-left: 0;
          margin-bottom: 0;
   }
   .custom--tabs .tab__list{
          display: inline-block;
          border-radius: 2px 2px 0px 0px;
  }

     /*  .custom--tabs .tab__list:not(:last-child){
        margin-right: 5px;
      } */
   /* coded by https://websensepro.com team */
  .custom__tab-content{
      display:none;
      background-color: #ffffff;
      border-radius: 2px;
      padding: 30px;
  }
   .custom__tab-content.active {
      display:block;
      padding: 30px;
    }
    .tab__list h6.custom-tab{
      display:inline-block;
      padding:13px 15px;
      cursor: pointer;
      margin: 0px;
      font-size: 20px;
      line-height: normal;
      transition: 0.3s all linear;
      border-bottom: 1px solid;
      text-align: center;
      text-transform: uppercase;
      width: 100%;
    }
      .active-always{
      display: block;
      }
      .tab__list h6.custom-tab.active
      {
      border-bottom: 3px solid #000;
      border-radius: 2px 2px 0px 0px;
      padding-bottom: 12px;
      }
      .product-tab-image img{
        width: 100%;
        height: auto;
        border-radius: 2px 2px 0px 0px;
      }

      .product-tab-block .custom--tabs{
        justify-content: space-between;
        flex-wrap: nowrap;
        padding: 10px 0;
      }
      .custom--tabs .tab__list{
        width: 100%;
      }
       @media only screen and (max-width: 749px){
        .tab__list h6.custom-tab{
          font-size: 14px;
        }
        }
          
      @media only screen and (max-width: 576px)
      {
      ul.custom--tabs{
      display: flex;
      padding-left: 0;
      justify-content: center;
      }
      .custom--tabs .tab__list .custom-tab{
      margin: 0px;
      width: 100%;
      letter-spacing: 0.2px;
      }
      .tab__list h6.custom-tab
      {
      padding: 10px 0px;
      margin: 10px 0px;
      }
      .custom__tab-content.active{
      padding: 10px 15px 10px;
      }
        .tab__list h6.custom-tab{
          font-size: 14px;
        }
        .tab__list h6.custom-tab.active{
          padding-bottom: 9px;
        }
        .product-tab-block .custom--tabs{
          padding: 4px 0;
        }
      }

     

     @media(max-width: 749px) {
      .product-tab-image.desktop-image{display:none !important;}
      .product-tab-image.mobile-image{display:block !important;}
      }
      @media(min-width: 750px) {
      .product-tab-image.mobile-image {display:none !important;}
      .product-tab-image.desktop-image{display:block !important;}
      } 
       @media only screen and (max-width: 400px){
        .tab__list h6.custom-tab{
          font-size: 14px;
        }
      }
        {%- endstyle -%}
        
        {%- capture sizes -%}
  {% if section.settings.full_width -%}
    100vw
  {%- else -%}
    (min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 100 }}px, (min-width: 750px)
    calc(100vw - 10rem), 100vw
  {%- endif %}
{%- endcapture -%}

<div class="product-tabs-section  section-{{ section.id }}-padding gradient color-{{ section.settings.section_color_scheme }} gradient{% if section.settings.title == blank %} no-heading{% endif %} {{ section.settings.custom_class_name }}">
<div class="product-tab-block">
<ul class="custom--tabs">
  <!-- coded by https://websensepro.com team -->
  {% for block in section.blocks %}
    {% if block.type == 'tab' %}
      <li class="tab__list {% if forloop.first %}active{% endif %}">
        <h6
          class="custom-tab"
          data-content-id="content--{{ block.id }}--{{ section.id }}"
        >
          {{ block.settings.heading }}
        </h6>
      </li>
    {% endif %}
  {% endfor %}
</ul>

  <div class="product-content-block page-width">

{% for block in section.blocks %}
  {% if block.type == 'tab' %}
    <div
      id="content--{{ block.id }}--{{ section.id }}"
      class="custom__tab-content {% if forloop.first %}active{% endif %}"
    >
      {% if block.settings.tab_content != blank %}
        {{ block.settings.tab_content }}
      {% endif %}

       {%- if block.settings.cover_image != blank -%}
         <div class="product-tab-image desktop-image">
          {{
             block.settings.cover_image
            | image_url: width: 3840
            | image_tag: sizes: sizes, widths: '375, 750, 1100, 1500, 1780, 2000, 3000, 3840', alt: alt
          }}
         </div>
        {%- endif -%}

      {%- if block.settings.cover_mobile_image != blank -%}
         <div class="product-tab-image mobile-image">
          {{
             block.settings.cover_mobile_image
            | image_url: width: 900
            | image_tag: sizes: sizes, widths: '375, 750, 1100, 1500, 1780, 2000, 3000, 3840', alt: alt
          }}
         </div>
        {%- endif -%}

      {% if block.settings.page.content != blank and block.settings.page != blank %}
        {{ block.settings.page.content }}
      {% endif %}
    </div>
  {% endif %}
{% endfor %}

  </div>

</div>
  </div>

{% javascript %}
  const tabs = document.querySelectorAll('.custom-tab');
  const tabContents = document.querySelectorAll('.custom__tab-content');

  // Initially set the first tab and content as active
  if (tabs.length > 0 && tabContents.length > 0) {
      tabs[0].classList.add('active');
      tabContents[0].classList.add('active');
  }

  tabs.forEach(function(tab){
      tab.addEventListener('click', function() {
          const contentId = this.dataset.contentId;
          const content = document.getElementById(contentId);

          // Remove 'active' class from all tabs and contents
          tabContents.forEach(function(content) {
              content.classList.remove('active');
          });
          tabs.forEach(function(tab) {
              tab.classList.remove('active');
          });

          // Add 'active' class to the clicked tab and its content
          this.classList.add('active');
          content.classList.add('active');
      });
  });
{% endjavascript %}


{% schema %}
{
  "name": "t:sections.product-tabs.name",
  "settings": [
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4, 
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    },
    {
      "type": "color_scheme",
      "id": "section_color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    }
],
   "blocks": [
    {
      "type": "tab",
      "name": "t:sections.product-tabs.blocks.tab.name",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "t:sections.product-tabs.blocks.tab.settings.heading.label",
          "info": "Include a heading that explains the tab."
        },
         {
          "type": "richtext",
          "id": "tab_content",
          "label": "t:sections.product-tabs.blocks.tab.settings.tab-content.label"
        },
        {
          "type": "image_picker",
          "id": "cover_image",
          "label": "t:sections.product-tabs.blocks.tab.settings.cover_image.label"
       },
        {
          "type": "image_picker",
          "id": "cover_mobile_image",
          "label": "t:sections.product-tabs.blocks.tab.settings.cover_mobile_image.label"
       },
         {
        "type": "page",
        "id": "page",
        "label": "t:sections.product-tabs.blocks.tab.settings.page.label"
       },
      ]
    }
  ],
   "presets": [
    {
      "name": "t:sections.product-tabs.presets.name",
      "blocks": [
        {
          "type": "tab"
        }
      ]
    }
  ]
}
{% endschema %}